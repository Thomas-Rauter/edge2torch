tests/module/ should test internal contracts:

Does this validator reject malformed internal state?
Does this execution-plan builder handle edge cases?
Does this model class raise the right internal errors?
Do helper functions behave correctly?
